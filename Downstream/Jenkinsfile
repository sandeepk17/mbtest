pipeline {
  agent{
      // Set Build Agent as Docker file 
      dockerfile true
    }
    environment {
        PROJECT_NAME = "ngraph"
    }

    stages {
        stage ("downstream") {
            steps {
                echo"--------Testing jobs---------------"
                //script {
                //    killPreviousRunningJobs()
                //}
                currentBuild.displayName = 'This build needs help!!!'
                buildDescription("Committer: ${PROJECT_NAME}")
            }
        }
        stage ("Regression testing") {
            steps {
                echo"--------Testing jobs---------------"
                //sleep(time:100,unit:"SECONDS")
            }
        }
    }
    post {
        failure {
           echo"--------failing jobs-----------" 
        }
        cleanup {
            echo"--------deleting repo-----------" 
            //deleteDir()
        }
    }
}